# Node.js dependencies
# Typically large and re-installed using package.json/package-lock.json
node_modules/
archive

# Environment variables file
# IMPORTANT: Contains secrets like API keys, DB URIs, tokens! NEVER commit this.
.env
.env.*
!.env.example

# Log files
# Often environment-specific and can grow large
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pids/
*.pid
*.seed
*.pid.lock

# Build output / Distribution folders
# Common output directories for builds (e.g., TypeScript compilation, frontend bundles)
dist/
build/
out/
.parcel-cache/

# Generated CSS (Optional)
# If your styles.css is always generated by 'npm run build:css' before deployment,
# you might want to ignore it. If you commit the built CSS, remove/comment this line.
# public/css/styles.css

# Test coverage reports
coverage/
.nyc_output/
*.lcov

# IDE / Editor specific files
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Operating System generated files
.DS_Store
Thumbs.db

# Optional temporary files/folders
temp/
tmp/